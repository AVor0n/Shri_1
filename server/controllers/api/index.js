const deleteImage = require('./deleteImage');
const getImage = require('./getImage');
const getImages = require('./getImages');
const merge = require('./merge');
const { upload, uploader } = require('./upload');

module.exports = {
  deleteImage,
  getImage,
  getImages,
  merge,
  upload,
  uploader,
};
